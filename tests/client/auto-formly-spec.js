describe('autoFormly', function () {
    var autoFormly;

    beforeEach(function () {
        module('autoFormly');

        inject(function (_autoFormly_) {
            autoFormly = _autoFormly_;
        });
    });
    
    
});